Steps to install/run Jekyll on Raspberry PI
-------------------------------------------

$ sudo apt-get install ruby1.9.1-dev

$ sudo apt-get install nodejs

$ sudo gem install execjs

$ sudo gem install jekyll

$ jekyll new www-manwar-org

$ cd www-manwar-org

$ jekyll serve
